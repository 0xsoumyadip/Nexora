import { DocumentBrowser } from "@/features/browser"; import { listDocuments } from "@/lib/data/documents"; export default async function Page(){return <DocumentBrowser initial={await listDocuments("recent")} scope="recent"/>}
