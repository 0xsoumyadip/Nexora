"use client"; import { useState } from "react"; import { ThemeToggle } from "@/components/theme-toggle"; export default function Page(){const[t,setT]=useState("Profile");return <div className="app-content settings"><div className="page-title"><div><h1>Settings</h1><p>Manage your personal preferences.</p></div></div><div className="tabs">{["Profile","Appearance","Notifications"].map(x=><button className={t===x?"active":""} onClick={()=>setT(x)} key={x}>{x}</button>)}</div>{t==="Appearance"?<div><h2>Appearance</h2><p className="muted">Choose the color mode that feels best for you.</p><ThemeToggle/></div>:<div><h2>{t}</h2><p className="muted">This is a UI-only settings placeholder, ready for your real account data.</p></div>}</div>}
